import random

def funny_name(names):

    for name in names:
        pass
    

    first_name = name.split(" ")[0]
    last_name = name.split(" ")[1]

    random_number = random.randint(1,2)
